ALPHA ENGINE V13 — PHASE 0

Purpose: start V13 as a separate research project without deleting V12, while hard-blocking 2025+ from research selection and hard-blocking V12 model/policy/OOS artifacts as research inputs.

Extract this ZIP directly into the existing V12 project root, then run:

  Set-ExecutionPolicy -Scope Process Bypass
  .\RUN_V13_PHASE0.ps1

It creates a sibling folder:
  ALPHA_ENGINE_V13_RETURN_FIRST_MULTI_HORIZON

Expected outputs there:
  outputs\v13_phase0_summary.json
  outputs\v13_phase0_gate.csv
  outputs\v13_phase0_source_manifest.json

Do not delete V12. It remains an immutable historical benchmark/evidence trail.
